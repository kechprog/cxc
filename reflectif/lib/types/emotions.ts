/**
 * Emotion types for Reflectif
 *
 * TODO: Implement after we know Hume.ai's actual output format and emotion categories.
 *
 * Expected exports:
 * - EmotionType (union of emotion names)
 * - EmotionScores (Record<EmotionType, number>)
 * - TimestampedEmotionScores ({ timestamp: number, scores: EmotionScores })
 * - EMOTION_COLORS (visualization colors)
 * - EMOTION_DEFINITIONS (human-readable descriptions)
 */
